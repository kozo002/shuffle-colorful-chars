{
  "name": "shuffle-colorful-chars",
  "version": "1.0.0",
  "description": "Shuffle characters",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "./node_modules/.bin/watchify lib/index.js -o lib/bundle.js -v -t [ babelify --presets [ es2015 ] ]",
    "watch-test": "./node_modules/.bin/watchify lib/test.js -o lib/bundle-test.js -v -t [ babelify --presets [ es2015 ] ]"
  },
  "author": "kozo yamagata",
  "license": "ISC",
  "dependencies": {
    "babel-preset-es2015": "^6.18.0",
    "babelify": "^7.3.0",
    "browserify": "^13.1.1",
    "watchify": "^3.7.0"
  }
}
